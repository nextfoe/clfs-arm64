- add perf, ftrace

- setup network
- move source code to repo dir
- port
  - gcc,
  - vim,
  - cscope,
  - tmux,
- port X window?
- run android ?
- kvm?

tips:

  test -d vim || git clone https://github.com/vim/vim.git || exit
  test -d gcc || git clone https://github.com/gcc-mirror/gcc.git || exit
