- make ldd available
- port
  - bash,
  - gdb,
  - gcc,
  - vim,
  - cscope,
  - tmux,
- setup network

- port X window?
- run android ?
- kvm?

tips:

  test -d vim || git clone https://github.com/vim/vim.git || exit
  test -d binutils-gdb || git clone git://sourceware.org/git/binutils-gdb.git || exit
